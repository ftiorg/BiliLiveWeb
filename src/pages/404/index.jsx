import React, {Component} from 'react';
import './style.scss';

class NotMatch extends Component {
    render() {
        return (
            <p>not found</p>
        );
    }
}

export default NotMatch;
